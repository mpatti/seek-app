{
  "name": "seek-app",
  "version": "1.0.0",
  "scripts": {
    "build": "sed -i 's/ESV_API_KEY_PLACEHOLDER/'$ESV_API_KEY'/g; s/OPENAI_API_KEY_PLACEHOLDER/'$OPENAI_API_KEY'/g' index.html && mkdir -p dist && cp index.html dist/"
  }
}
